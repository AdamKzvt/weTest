{% extends 'base.html.twig' %}

{% block title %}Hello SimulationController!{% endblock %}

{% block body %}

<div id="layoutSidenav_content">
                <main>
                    <div class="container">
                        <h1 class="mt-4 text-center">Simulation d'IOT</h1>
                        <div class="card mb-4">
                            <div class="card-body">

                                {{ form_start(form) }}
                                {{ form_widget(form) }}
                                <button type="submit">Simuler</button>
                                {{ form_end(form) }}
                                
                            </div>
                        </div>
                        </main> 

                

</div>
{% endblock %}
